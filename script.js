document.addEventListener("DOMContentLoaded", function () {

    console.log("Nextcomm - site carregado.");

    /*
     * O widget da WTS é carregado pelo script
     * instalado no <head> do index.html.
     *
     * Os elementos que possuem:
     *
     *     class="h-widget-trigger"
     *
     * são utilizados pelo próprio widget
     * para abrir o atendimento.
     */

});
